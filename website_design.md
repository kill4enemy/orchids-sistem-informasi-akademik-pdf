<user_request>buatkan saya tampilan website yang simpel seperti file pdf yang tertera. Sistem Informasi akademik</user_request>

<todo_list>
1. Create database schema and types for Murid (students), Kelas (classes), Guru (teachers), and Pengguna (users) with proper TypeScript interfaces
2. Build authentication system with login/logout pages and role-based access (Admin vs Guru) using NextAuth or similar
3. Create admin dashboard with statistics cards showing total murid, kelas, and guru counts, plus navigation sidebar
4. Implement CRUD pages for managing Murid data (add, edit, delete, view list with search by name/NISN/class) and Kelas data (assign wali kelas)
5. Add laporan (reports) page that displays student data in a printable PDF format and ensure responsive layout with simple, clean Indonesian UI throughout
</todo_list>